/**
 * MyWallet — /breakdown Route
 *
 * Renders the Available to Spend Breakdown Screen.
 * Accessible by tapping the hero card on the Home Dashboard.
 */

import BreakdownScreen from '@/screens/BreakdownScreen';

export default BreakdownScreen;
