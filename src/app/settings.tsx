/**
 * MyWallet — Settings Route
 * 
 * Expo Router route component wrapping SettingsScreen.
 */

import React from 'react';
import { SettingsScreen } from '@/screens';

export default function SettingsRoute() {
  return <SettingsScreen />;
}
